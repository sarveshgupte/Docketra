/**
 * Docket Controller
 *
 * Canonical re-export of case.controller under the Docket naming convention.
 * All new code should import from this file.
 * The underlying implementation remains in case.controller.js for backward
 * compatibility with existing imports and tests.
 */

module.exports = require('./case.controller');
