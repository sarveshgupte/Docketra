# PR 2 — PlatformShell polish

- Rebalanced workspace shell spacing, topbar hierarchy, and action rail structure in `PlatformShell`.
- Quieted breadcrumbs and only render them when they add orientation context.
- Improved sidebar grouping clarity, active state visibility, and collapsed/mobile shell behavior while preserving route access and role visibility logic.
- Added shell-focused static regression coverage for landmarks, navigation wiring, direct workbasket links, and CSS shell polish hooks.
